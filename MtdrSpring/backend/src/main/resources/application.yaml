spring:
  # H2 Database Configuration
  datasource:
    url: jdbc:h2:mem:todo_db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # Hibernate Configuration
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  # Enable the H2 Web Console
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    root: info
    org.hibernate.SQL: DEBUG

# Don't forget your DeepSeek and Telegram keys!
deepseek:
  api:
    key: sk-test
    url: https://api.deepseek.com/v1/chat/completions

telegram:
  bot:
    token: 8627527546:AAHSXKQjW097kI2JYDhWW6RMuUa19d1nemU
    name: OCI_Task_Bot
