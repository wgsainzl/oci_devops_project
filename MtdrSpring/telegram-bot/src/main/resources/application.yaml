server:
  port: 8081

telegram:
  bot:
    name: ${TELEGRAM_BOT_NAME}
    token: ${TELEGRAM_BOT_TOKEN}

deepseek:
  api:
    key: ${DEEPSEEK_API_KEY}
    url: ${DEEPSEEK_URL}
    model: ${DEEPSEEK_MODEL:phi3:mini}

backend:
  url: ${BACKEND_URL:http://localhost:8080}
